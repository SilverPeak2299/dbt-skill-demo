name: "southern_cross_bank_demo"
version: "1.0.0"
config-version: 2

profile: "southern_cross_bank_demo"

model-paths:
  - "models"
analysis-paths:
  - "analyses"
test-paths:
  - "tests"
seed-paths:
  - "seeds"
macro-paths:
  - "macros"
snapshot-paths:
  - "snapshots"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  southern_cross_bank_demo:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
